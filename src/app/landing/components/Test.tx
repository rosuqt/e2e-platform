import { motion } from "framer-motion";

interface FeatureSectionProps {
  title: string;
  subtitle: string;
  description: string;
  buttonText: string;
  imageSrc: string;
}

const FeatureSection: React.FC<FeatureSectionProps> = ({
  title,
  subtitle,
  description,
  buttonText,
  imageSrc,
}) => {
  return (
    <motion.div 
      className="mt-36 pt-9 pb-10 flex flex-col lg:flex-row justify-between items-center"
      initial={{ opacity: 0, y: 50 }}
      animate={{ opacity: 1, y: 0 }}
      transition={{ duration: 0.8 }}
    >
      <motion.div
        className="mr-6 text-left flex flex-col items-start max-w-[900px] space-y-4"
        initial={{ opacity: 0, x: -50 }}
        animate={{ opacity: 1, x: 0 }}
        transition={{ duration: 0.8, delay: 0.3 }}
      >
        <h1 className="text-5xl font-bold md:ml-8 px-5">
          <span className="text-customYellow">{title} </span>
          <span className="text-darkBlue">{subtitle}</span>
        </h1>
        <p className="mt-4 text-gray-500 text-lg md:ml-8 pt-4 px-5 w-[600px]">
          {description}
        </p>
        <motion.button 
          className="bg-button text-white py-4 rounded-[10px] mt-auto ml-12 px-14 shadow-lg"
          whileHover={{ scale: 1.05 }}
          whileTap={{ scale: 0.95 }}
        >
          {buttonText}
        </motion.button>
      </motion.div>
      
      <motion.div 
        className="bg-gray-300 w-[400px] h-[520px] rounded-[30px] mr-44"
        initial={{ opacity: 0, x: 50 }}
        animate={{ opacity: 1, x: 0 }}
        transition={{ duration: 0.8, delay: 0.3 }}
      >
        <img src={imageSrc} alt="Feature Image" className="w-full h-full object-cover rounded-[30px]" />
      </motion.div>
    </motion.div>
  );
};

export default FeatureSection;
